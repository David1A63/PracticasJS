let contador = 0;

// El ciclo do while verifica la condición al final del ciclo
// A diferencia del ciclo while, este se ejecuta al menos una vez
do{
    console.log(contador);
    contador++;
}while(contador<3);

console.log('Fin del Ciclo While');