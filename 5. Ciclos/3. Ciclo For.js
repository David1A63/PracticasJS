// La declaración del contador solo se ejecutará solo una vez,
// Mientras que la condicion y el contador se ejecutará cada vez en el ciclo
for(let contador = 0; contador<3; contador++){
    console.log(contador);
}
console.log('Fin del Ciclo For');